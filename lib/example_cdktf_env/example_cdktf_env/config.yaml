# Example global configuration file
# Stacks can also have local config.yaml for over-rides and stack specific
# settings.

dev:
  regions:
    us-east-1:
      vpc_name: example-dev-vpc

qa:
  regions:
    us-east-1:
      vpc_name: example-qa-vpc

prod:
  regions:
    us-east-1:
      vpc_name: example-prod-vpc
